import { ContentElement, ContentImageProps } from "./types";
export declare const useContentImageUrl: ({ id, contentUrl, size }: ContentImageProps) => string;
export declare const ImageComponent: ContentElement;
