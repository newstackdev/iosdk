import { newcounter } from "./newcounter";
import { app } from "./app/";

// add overmind modules here
// these can be either custom modules for the app of in future plugins provided by the iosdk ecosystem
export default {
    app,
    newcounter // a very basic state demo
};
